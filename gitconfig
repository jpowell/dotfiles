[user]
	name = Justin Powell
	email = jpowellcs@gmail.com
[credential]
  helper = osxkeychain
[color]
  branch = auto
  status = auto
  diff = auto
  ui = auto
[core]
  autocrlf = input
  editor = vim
  pager = "less -F -X"
[diff]
  noprefix = true
	algorithm = patience
[push]
  default = simple
[commit]
	gpgsign = false
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
[pull]
	ff = only
